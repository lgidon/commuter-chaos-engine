repos:
-   repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.3.0
    hooks:
    -   id: ruff
        args: [ --fix ]  # Automatically fix what it can!
    -   id: ruff-format  # Ensure your code is pretty
